# from django.urls import path

# from apps.registration_profile.models import RegistrationProfile

urlpatterns = [
    # path('', RegistrationProfile.as_view()),
    # path('validation/', foo.as_view()),
]
